<Application x:Class="AFMediaBar.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnExplicitShutdown">
  <Application.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <!-- 语言词典由 App 启动时按设置替换（MergedDictionaries[0]） -->
        <ResourceDictionary Source="Resources/Languages/zh-CN.xaml" />
      </ResourceDictionary.MergedDictionaries>

      <SolidColorBrush x:Key="TaskbarPrimaryTextBrush" Color="#FFF7FA" />
    <SolidColorBrush x:Key="TaskbarSecondaryTextBrush" Color="#B9D1E3" />
    <SolidColorBrush x:Key="TaskbarSurfaceBrush" Color="#2AFFFFFF" />
    <SolidColorBrush x:Key="TaskbarHoverBrush" Color="#24FFFFFF" />
    <SolidColorBrush x:Key="TaskbarPressedBrush" Color="#38FFFFFF" />
    <SolidColorBrush x:Key="TaskbarDividerBrush" Color="#28FFFFFF" />
    <SolidColorBrush x:Key="TaskbarHighlightTextBrush" Color="#FFF7FA" />
    <SolidColorBrush x:Key="TaskbarDisabledTextBrush" Color="#66FFF7FA" />
    <SolidColorBrush x:Key="TaskbarReadabilityBrush" Color="#00000000" />
    <SolidColorBrush x:Key="PlayerPreviewBackgroundBrush" Color="#FF202020" />
    <SolidColorBrush x:Key="MenuBackgroundBrush" Color="#F0172632" />
    <SolidColorBrush x:Key="MenuPrimaryTextBrush" Color="#FFF7FA" />
    <SolidColorBrush x:Key="MenuSecondaryTextBrush" Color="#B9D1E3" />
    <SolidColorBrush x:Key="MenuBorderBrush" Color="#4DFFFFFF" />
    <SolidColorBrush x:Key="MenuHoverBrush" Color="#3D6C8794" />
    <SolidColorBrush x:Key="MenuPressedBrush" Color="#526C8794" />
    <SolidColorBrush x:Key="MenuSelectionBrush" Color="#4D6F9AAA" />
    <SolidColorBrush x:Key="MenuHighlightTextBrush" Color="#FFF7FA" />
    <SolidColorBrush x:Key="MenuDisabledBrush" Color="#66B9D1E3" />
    <SolidColorBrush x:Key="MenuSeparatorBrush" Color="#28FFFFFF" />
    <SolidColorBrush x:Key="SliderFillBrush" Color="#B96F9AAA" />
    <SolidColorBrush x:Key="SliderTrackBrush" Color="#30FFFFFF" />
    <SolidColorBrush x:Key="SliderThumbBrush" Color="#FFF7FA" />
    <SolidColorBrush x:Key="SliderThumbBorderBrush" Color="#806F9AAA" />
    <SolidColorBrush x:Key="LayoutEditorCanvasBrush" Color="#FF232B34" />
    <SolidColorBrush x:Key="LayoutEditorDropBrush" Color="#36569CFF" />
    <SolidColorBrush x:Key="LayoutEditorAccentBrush" Color="#FF8AB4F8" />
    <SolidColorBrush x:Key="LayoutEditorInvalidBrush" Color="#33FF5555" />
    <DropShadowEffect x:Key="TaskbarTextEffect"
                      Color="#99000000"
                      BlurRadius="1"
                      Direction="0"
                      ShadowDepth="0"
                      Opacity="0.82" />
    <SolidColorBrush x:Key="{x:Static SystemColors.MenuBrushKey}" Color="#F0172632" />
    <SolidColorBrush x:Key="{x:Static SystemColors.MenuTextBrushKey}" Color="#FFF7FA" />
    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#3D6C8794" />
    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#FFF7FA" />
    <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#F0172632" />
    <FontFamily x:Key="AppTextFontFamily">Segoe UI Variable Text, Segoe UI</FontFamily>
    <FontFamily x:Key="AppDisplayFontFamily">Segoe UI Variable Display, Segoe UI</FontFamily>
    <FontWeight x:Key="PlayerTitleFontWeight">SemiBold</FontWeight>
    <FontWeight x:Key="PlayerTextFontWeight">Normal</FontWeight>
    <FontFamily x:Key="AppIconFontFamily">Segoe Fluent Icons, Segoe MDL2 Assets</FontFamily>

    <Style TargetType="ContextMenu">
      <Setter Property="Background" Value="{DynamicResource MenuBackgroundBrush}" />
      <Setter Property="BorderBrush" Value="{DynamicResource MenuBorderBrush}" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Padding" Value="4" />
      <Setter Property="HasDropShadow" Value="True" />
      <Setter Property="FontFamily" Value="{DynamicResource AppTextFontFamily}" />
      <Setter Property="FontSize" Value="13" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ContextMenu">
            <Border Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="8"
                    Padding="{TemplateBinding Padding}"
                    SnapsToDevicePixels="True">
              <ItemsPresenter />
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="MenuItem">
      <Setter Property="Foreground" Value="{DynamicResource MenuPrimaryTextBrush}" />
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderBrush" Value="Transparent" />
      <Setter Property="Padding" Value="6,5" />
      <Setter Property="Margin" Value="1,1" />
      <Setter Property="FontFamily" Value="{DynamicResource AppTextFontFamily}" />
      <Setter Property="FontSize" Value="13" />
      <Setter Property="HorizontalAlignment" Value="Stretch" />
      <Setter Property="HorizontalContentAlignment" Value="Stretch" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="MenuItem">
            <Grid SnapsToDevicePixels="True">
              <Border x:Name="ItemBorder"
                      MinHeight="31"
                      Padding="{TemplateBinding Padding}"
                      Background="{TemplateBinding Background}"
                      BorderBrush="{TemplateBinding BorderBrush}"
                      BorderThickness="1"
                      CornerRadius="5">
                <Grid>
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="22" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="17" />
                  </Grid.ColumnDefinitions>
                  <TextBlock x:Name="CheckGlyph"
                             Grid.Column="0"
                             Text="&#xE73E;"
                             FontFamily="{StaticResource AppIconFontFamily}"
                             FontSize="12"
                             Foreground="{DynamicResource MenuPrimaryTextBrush}"
                             HorizontalAlignment="Center"
                             VerticalAlignment="Center"
                             Visibility="Collapsed" />
                  <ContentPresenter x:Name="IconPresenter"
                                    Grid.Column="0"
                                    ContentSource="Icon"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center" />
                  <ContentPresenter Grid.Column="1"
                                    ContentSource="Header"
                                    RecognizesAccessKey="True"
                                    VerticalAlignment="Center" />
                  <TextBlock x:Name="SubmenuGlyph"
                             Grid.Column="2"
                             Text="&#xE76C;"
                             FontFamily="{StaticResource AppIconFontFamily}"
                             FontSize="10"
                             Foreground="{DynamicResource MenuSecondaryTextBrush}"
                             HorizontalAlignment="Right"
                             VerticalAlignment="Center"
                             Visibility="Collapsed" />
                </Grid>
              </Border>
              <Popup x:Name="PART_Popup"
                     AllowsTransparency="True"
                     Focusable="False"
                     HorizontalOffset="-2"
                     IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                     Placement="Right"
                     PlacementTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                     PopupAnimation="Fade"
                     StaysOpen="False">
                <Border Width="230"
                        Padding="3"
                        Background="{DynamicResource MenuBackgroundBrush}"
                        BorderBrush="{DynamicResource MenuBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="7"
                        SnapsToDevicePixels="True">
                  <ScrollViewer CanContentScroll="True"
                                HorizontalScrollBarVisibility="Disabled"
                                VerticalScrollBarVisibility="Auto">
                    <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle" />
                  </ScrollViewer>
                </Border>
              </Popup>
            </Grid>
            <ControlTemplate.Triggers>
              <Trigger Property="IsHighlighted" Value="True">
                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource MenuHoverBrush}" />
                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="{DynamicResource MenuBorderBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource MenuHighlightTextBrush}" />
              </Trigger>
              <Trigger Property="IsSubmenuOpen" Value="True">
                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource MenuHoverBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource MenuHighlightTextBrush}" />
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{DynamicResource MenuDisabledBrush}" />
                <Setter TargetName="ItemBorder" Property="Opacity" Value="0.58" />
              </Trigger>
              <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="CheckGlyph" Property="Visibility" Value="Visible" />
                <Setter TargetName="IconPresenter" Property="Visibility" Value="Collapsed" />
              </Trigger>
              <Trigger Property="HasItems" Value="True">
                <Setter TargetName="SubmenuGlyph" Property="Visibility" Value="Visible" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="Separator">
      <Setter Property="Height" Value="1" />
      <Setter Property="Margin" Value="8,5" />
      <Setter Property="Background" Value="{DynamicResource MenuSeparatorBrush}" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Separator">
            <Border Background="{TemplateBinding Background}" Height="1" />
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="OutputDeviceListItemStyle" TargetType="ListBoxItem">
      <Setter Property="Foreground" Value="{DynamicResource MenuPrimaryTextBrush}" />
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="Padding" Value="8,7" />
      <Setter Property="Margin" Value="2,1" />
      <Setter Property="HorizontalContentAlignment" Value="Stretch" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ListBoxItem">
            <Border x:Name="ItemBorder"
                    Background="{TemplateBinding Background}"
                    CornerRadius="5"
                    Padding="{TemplateBinding Padding}">
              <ContentPresenter />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource MenuHoverBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource MenuHighlightTextBrush}" />
              </Trigger>
              <Trigger Property="IsSelected" Value="True">
                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource MenuSelectionBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource MenuHighlightTextBrush}" />
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.5" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="VolumeSliderStyle" TargetType="Slider">
      <Setter Property="Focusable" Value="False" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Slider">
            <Grid Width="28"
                  Height="{TemplateBinding Height}"
                  HorizontalAlignment="Center">
              <Track x:Name="PART_Track"
                     Margin="0,7"
                     Orientation="Vertical"
                     IsDirectionReversed="{TemplateBinding IsDirectionReversed}">
                <Track.DecreaseRepeatButton>
                  <RepeatButton Command="{x:Static Slider.DecreaseLarge}"
                                Focusable="False">
                    <RepeatButton.Template>
                      <ControlTemplate TargetType="RepeatButton">
                        <Border Width="4"
                                HorizontalAlignment="Center"
                                Background="{DynamicResource SliderFillBrush}"
                                CornerRadius="2" />
                      </ControlTemplate>
                    </RepeatButton.Template>
                  </RepeatButton>
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                  <Thumb Width="15" Height="15">
                    <Thumb.Template>
                      <ControlTemplate TargetType="Thumb">
                        <Border Background="{DynamicResource SliderThumbBrush}"
                                BorderBrush="{DynamicResource SliderThumbBorderBrush}"
                                BorderThickness="2"
                                CornerRadius="7.5" />
                      </ControlTemplate>
                    </Thumb.Template>
                  </Thumb>
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                  <RepeatButton Command="{x:Static Slider.IncreaseLarge}"
                                Focusable="False">
                    <RepeatButton.Template>
                      <ControlTemplate TargetType="RepeatButton">
                        <Border Width="4"
                                HorizontalAlignment="Center"
                                Background="{DynamicResource SliderTrackBrush}"
                                CornerRadius="2" />
                      </ControlTemplate>
                    </RepeatButton.Template>
                  </RepeatButton>
                </Track.IncreaseRepeatButton>
              </Track>
            </Grid>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="TransportButtonStyle" TargetType="Button">
      <Setter Property="Width" Value="36" />
      <Setter Property="Height" Value="36" />
      <Setter Property="Margin" Value="1,0" />
      <Setter Property="Padding" Value="0" />
      <Setter Property="Foreground" Value="{DynamicResource TaskbarPrimaryTextBrush}" />
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="FocusVisualStyle" Value="{x:Null}" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" CornerRadius="4">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource TaskbarHoverBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource TaskbarHighlightTextBrush}" />
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource TaskbarPressedBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource TaskbarHighlightTextBrush}" />
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.35" />
                <Setter Property="Foreground" Value="{DynamicResource TaskbarDisabledTextBrush}" />
                <Setter Property="Cursor" Value="Arrow" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="PlayPauseExampleSkinStyle"
           TargetType="Button"
           BasedOn="{StaticResource TransportButtonStyle}">
      <Setter Property="BorderBrush" Value="{DynamicResource TaskbarHighlightTextBrush}" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Background" Value="{DynamicResource TaskbarSurfaceBrush}" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="SkinBorder"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="10">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="SkinBorder" Property="Background" Value="{DynamicResource TaskbarHoverBrush}" />
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="SkinBorder" Property="Background" Value="{DynamicResource TaskbarPressedBrush}" />
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.35" />
                <Setter Property="Cursor" Value="Arrow" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="LayoutEditorButtonStyle" TargetType="Button">
      <Setter Property="Foreground" Value="{DynamicResource MenuPrimaryTextBrush}" />
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderBrush" Value="{DynamicResource MenuBorderBrush}" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="FocusVisualStyle" Value="{x:Null}" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="ButtonBorder"
                    Padding="{TemplateBinding Padding}"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="4">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource MenuHoverBrush}" />
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource MenuPressedBrush}" />
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{DynamicResource MenuDisabledBrush}" />
                <Setter Property="Opacity" Value="0.55" />
                <Setter Property="Cursor" Value="Arrow" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    </ResourceDictionary>
  </Application.Resources>
</Application>
