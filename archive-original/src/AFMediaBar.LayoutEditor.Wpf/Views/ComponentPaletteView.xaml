<UserControl x:Class="AFMediaBar.LayoutEditor.Wpf.Views.ComponentPaletteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <TabControl ItemsSource="{Binding PaletteGroups}">
    <TabControl.ItemTemplate>
      <DataTemplate><TextBlock Text="{Binding DisplayName}" /></DataTemplate>
    </TabControl.ItemTemplate>
    <TabControl.ContentTemplate>
      <DataTemplate>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
          <ItemsControl ItemsSource="{Binding Items}">
            <ItemsControl.ItemsPanel>
              <ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <Button Width="96" Height="68" Margin="0,0,6,6" Padding="4"
                        Tag="{Binding Token}"
                        ToolTip="{Binding Description}"
                        PreviewMouseLeftButtonDown="PaletteButton_OnPreviewMouseLeftButtonDown"
                        PreviewMouseMove="PaletteButton_OnPreviewMouseMove"
                        Click="PaletteButton_OnClick">
                  <StackPanel IsHitTestVisible="False">
                    <Viewbox Width="72" Height="36" Stretch="Uniform">
                      <ContentControl Loaded="Preview_OnLoaded" />
                    </Viewbox>
                    <TextBlock Text="{Binding DisplayName}" Margin="2,2,2,0" FontSize="10"
                               TextAlignment="Center" TextTrimming="CharacterEllipsis" />
                  </StackPanel>
                </Button>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </ScrollViewer>
      </DataTemplate>
    </TabControl.ContentTemplate>
  </TabControl>
</UserControl>
